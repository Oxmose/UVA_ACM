#include <iostream>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	
	unsigned int H;
	while(cin >> H)
	{
		unsigned int W;
		cin >> W;
		vector
		if(W == H && H == 0) return 0;

		for(unsigned int i = 0; i < H; ++i)
		{
			for(unsigned int j = 0; j < W; ++j)
			{
				cin >> 
			}
		}
	}

	return 0;
}
